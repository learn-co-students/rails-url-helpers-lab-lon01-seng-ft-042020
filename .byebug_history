c
student
student.update(active: !student.active)
student
student.update(active: !student.active)
!student.active
student.active
student.update(active: true)
student.update
student.active(true)
student = Student.find(params[:id])

paramsA
c
@student = Student.find(params[:id])
params
